var inteiro = 425520546320 // tipo de dado: int 
var flutuante = 3.14 // tipo de dado: float
var texto = 'Olá, Cris' // tipo de dado: string
var booleano = true // tipo de dado: boolean
var array = [1, 2 , 3, 4] // tipo de dado: array
var objetoPessoa = {nome: 'Cris', idade: 40, tamanho: 1.70}
var nulo = null // tipo de dado: null



// Operadores

// Operadores Aritméticos

let x = 10;
let y = 4;

let soma = x + y ; // Adição (+)
let subtracao = x - y; // Subtração (-)
let multiplicacao = x * y; // Multiplicação (*)
let divisao = x / y; // divisão (/)
let restoDivisão = x % y; // resto da divisão (%)
let incremento = x++; // incremento (++)
let decremento = x--; // decremento(--)
// Operadores de Atribuição

let c = 10; // Atribuição simples
c += 5; // Equivale a c =  c +5;


// Operadores de Comparação
let igual = x === y; // Igual (===)
let naoIgual = x!== y; // Não igual (!==)
let maior = x > y; // Maior que (>)
let menor = x < y; // menor que (<)
let maiorIgual = x >= y; // maior ou igual (>=)
let menorIgual = x <= y; // menor ou igual (<=)

// Operadores Lógicos

let condicao1 = true;
let condicao2 = false;

let cond = condicao1 && condicao2; // E (&&)
let cond2 = condicao1 || condicao2; // OU (||)
let cond3 = !condicao1; // Negação (!)


// Operador Ternário
let idade = 33;
let podeBeber = (idade >= 18) ? 'Pode beber' : 'Não pode beber';


// Operadores de concatenação

let nome = 'Cris';
let sobrenome = 'Silva';

let nomeCompleto = nome + '' + sobrenome; // Concatenação (+)